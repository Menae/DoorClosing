# Astra運用文書の監査・改善

2026-09-15 / DOCS-06・WORK-006 / Codex task 01a0a383-4c35-73c0-b769-4876bb5eb297。対象はこのプロジェクトの指示・文書・2スキル。ゲーム側の開始版は `9210c30`、mainに既存差分なし。

## 根拠と適用範囲

ユーザー提供の「Rethinking skills and prompts for GPT-6 Astra」を主資料とする。要点は短く明確なスキルの適用条件、必要時に読む詳細、過剰な一律手順の削減、明確な判断境界と完了条件。

補助として [OpenAI公式 Model guidance のPrompting best practices](https://developers.openai.com/api/docs/guides/latest-model#prompting-best-practices) を2026-09-15に閲覧し、指示の競合監査・依頼の完遂・変更に見合う検証を確認した。添付記事そのものの公開URLは特定しておらず、このリンクを添付記事のURLとは扱わない。

改善は既存Markdownと参照を拡張する方針。新しい管理サービスや依存パッケージは不要。以下はこのリポジトリへの適用判断であり、公式がこのゲームの検証・モデル配分を指定したという意味ではない。

## 所見と変更

| 所見 | 変更・維持した境界 |
|---|---|
| 小さな修正でも仕様全文などを毎回読む指示 | START_HEREを目的別の参照表に変更。挙動変更時の関連規則・例外・決定確認は保持。全体像が不明なときは読む範囲を広げる |
| AGENTS・開始手順・スキルに似た手順が重複 | 入口、検証方針、接続手順の責務を整理。スキルから必要な資料へリンク。短いスキルを分割するためだけの新規referenceは作らない |
| PROJECT_STATUSにM1／M2／VISの古い「現在」「次」が混在 | 現在地・最新証拠・未受入を短くまとめ、元の本文はPROJECT_HISTORYへ保存。過去の担当・実行中表記は当時の記録と明記 |
| Unityスキルが旧hold経路を指示 | 現行click → commitへ訂正。直接SubmitActionと合成入力・OS入力の違いは保持 |
| DEVELOPMENTの「5テスト」と旧移行待ちの説明 | UnityAgentMenuの現行assembly指定・testNames filterに合わせ、件数は結果XMLから確認。旧試験は履歴と明記 |
| 毎単位の一律検証が過剰な再実行につながる | VALIDATIONで文書・ロジック・入力・描画・音・Player等の影響別に必要な証拠を指定。全体完成と部分検証は区別 |
| Solへの復帰文がAstra継続のユーザー指定と競合 | 通常運用案より最新指定を優先。依頼範囲の実装・関連検証・回帰修正・記録・保存まで継続 |
| 簡素化で必要な承認まで消す危険 | GAME-015の試作と正式採用を区別し、未承認の重大設計・新依存・破壊・公開・費用等は既存境界を保持 |

AGENTS本文は6,496→3,570文字、START_HEREは3,678→2,808文字、旧PROJECT_STATUSは279行から現在欄約40行へ整理。文字数は改行をLFとして数えた比較で、token数・速度・利用枠の削減実績ではない。履歴保存で総ファイル量が減ることを目的にしていない。

## 検証

18文書の相対リンク48件・見出し参照7件は不備0。元のPROJECT_STATUS本文のbyte一致とゲーム側ファイルの無変更を確認。2スキルのquick_validateはUTF-8指定で合格（初回の既定cp932による読込失敗を解消）。機械確認は `artifacts/docs-06/validation.json`、作業結果はPROJECT_STATUSのDOCS-06欄に記録。

指示の読み合わせでは、文書の誤字修正はEditor不要、入力回帰はclick経路、材質は実画像と影響する回帰、Astraの継続開発は関連検証・保存まで、未承認の公開は準備済みの具体案を示して実行前に確認、という分岐を確認した。これは静的な整合性確認であり、別モデルでの行動テストではない。

Unityコード・Assets・設定は変更対象外のため、今回のUnityテスト／Player buildは実施しない。過去の33件合格を今回の検証結果に流用しない。指示を使った将来のモデル行動や費用削減は未実測。

## プロジェクト外に残る所見

個人用 `~/.codex/AGENTS.md` には、不確実性全般を重要判断とする文、すべての重要確認で特定ツールを強制する文、広範囲の作業前の研究・計画承認がある。委任済みの可逆的作業でも停止を誘う可能性がある。今回のプロジェクト規約には既存委任が適用される範囲を明示した。

個人用ファイルの変更は他プロジェクトにも作用するため今回の編集対象に含めていない。見直す場合は「未承認の重大な選択」と「既に任された実装」の境界を整理する。インストール済みプラグイン・共通スキル・モデル設定も今回の変更対象外。
