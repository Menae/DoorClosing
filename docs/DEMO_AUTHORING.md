# ゲーム内の文章を書く

## M5の確認と調整（本編Homecoming）

- `Homecoming`でPlay。開始位置から振り返った「出口」の通路、オートロックを通ったホール右側の「階段」の通路は、曲がり角を歩くと元の場所へ戻る。夜・帰宅・死亡の判定には影響しない。
- 造作は `EntranceHall/入口_集合ポストとオートロック/退出通路_同じ入口へ` と `EntranceHall/階段通路_同じホールへ`。`HallwayLoop`の接続面と対称な通路形状は一組。片側だけ形や照明を変更すると接続が見える。入口の幅は1.8m。旧壁は非表示で保持。
- `演出調整_空間ごとの設備音`：入口／ホール／かご／8階の各ZoneでSource、聴取範囲、Level、切替秒数を調整。共通音量は従来の `音・動作調整 > 換気音`。各AudioSourceのClipを差し替えられる。非表示の階は鳴らない。旧デモの追従型環境音は維持。
- `NormalJourney > Cabin Power Presentation`：機械暴走時のかご照明の明るさと変化秒数。0.85／0.65、1.1秒は試作値。基本の階数上昇・停止判断は従来どおり。空間異常の天井伸長は別の演出。
- `テキスト編集/12_退出・階段の案内`：出口／階段の案内文。`14_住戸番号`：非操作住戸801〜804・806の番号。805の既存「自宅」文章は変更していない。室番号を読むことは怪異判別の必須条件ではない。
- `テキスト編集/13_スタッフロール_文章と表示時間`：ページの見出し・本文・順序・数、1ページの秒数、フェード秒数、ボタン文を編集。全4夜の結果画面→スタッフロール→タイトル。ボタンまたはEscで省略可能。長文はスクロールでき、省略ボタンは画面下に固定。素材クレジットは同梱licensesにも残す。

Play中の調整は保存されない。停止してInspectorを編集し、シーンを保存する。文章の最終執筆と怖さ・自然さの評価は作者が後から行う。

閉扉待機の確認: `Homecoming` または `M1NormalRoute` で乗車し、8階を押さず「閉」を押す。その場で扉が閉じたまま待機し、「開」で開扉、8階で出発する。敷居に立った際の安全再開扉と、本物8階到着後の再提示は従来どおり。

## 本編冒頭の制作（OPENING-001）

本編用は `Assets/Scenes/Homecoming.unity`。既存の `PlayableDemo` は保持し、自動同期しない。Homecomingでは導入説明の本文を出さず、805の郵便受けを調べてからオートロックへ805を入力するとホールへ進める。取消ボタンで入力を消す。誤番号は短い赤表示の後に再入力可能。翌夜と死亡再開はホールからで、郵便受けを繰り返し調べる必要はない。

- 文章編集→`09_入口・ポスト・オートロック`：ポスト番号・キー表記。文章の編集だけでは入力判定の番号は変わらない。
- `EntranceHall > 入口_集合ポストとオートロック > 入口操作_805を調べて入力`：有効番号、自動扉の速度・移動量・操作音量。
- 同じInspectorの **操作盤の配置・大きさを選択** から筐体のTransformへ移動できる。現在の幅205×高さ440mm、Scaleは各軸0.5。子のキー・文字・Colliderを一緒に縮小している。配置を変えるときは壁面への取付とクリックをPlayで確認する。
- `本編_入口と夜の進行`：開始のフェード秒数、翌夜までの暗転秒数。翌夜の説明メニューは本編では使用しない。文章編集03の項目は既存デモ用として保持。
- `Player`を選択→Inspectorの`Nearby Interaction Markers`：▲の大きさ、出現・消失秒数、浮遊幅（0で停止）と周期、注視時の拡大率。1080pを基準に解像度へ追従する。近距離・遮蔽判定は操作Raycastと同じ基準を使用。テンキーは盤全体で一つの▲を表示する。
- `EntranceHall > 入口_集合ポストとオートロック > 集合郵便受け_54戸_201-1006`：6列×9段、1階共用・2〜10階各6戸の54戸。番号は文章編集09から変更可能。`ポスト照明`で局所照明を調整できる。
- `旧ポスト_更新前_非表示`は更新前の配置を保管した非表示オブジェクト。ゲーム中には表示しない。再有効化するとポストが重複するため、通常の編集は54戸側で行う。

本編のタイトルは既存名「帰宅」を仮置き。新しい物語文・独白は追加していない。掲示は作者の現状を複製しており、三系統の判別と救済を十分伝えるかの文章評価は引き続き必要。

本編のWindows版は、保存後に `Tools > Unity Agent > Build Homecoming Windows` で作る。通常のビルド設定の入口は既存デモのまま保持し、このメニューでHomecomingだけを指定する。生成先は `artifacts/builds/<日時>/GraduationProject.exe`。エディター用テストコードを含まない通常ビルドで、公開・リリースではない。

### 全4夜を通す／怪異だけ比較する（M3-04）

`Homecoming` を開き、`Tools > Unity Agent > 本編の全4夜を設定` → **全4夜を通して遊ぶ** をONにしてPlay。導入の通常帰宅→基本3種→別表現3種→各系統1種と順番を抽選した最終夜へ進む。毎夜、自宅のドアを触って帰宅する。死亡は現在夜のホールから、最終夜は再乗車時に再抽選。保存・再開は下記M4-01参照。

同じInspectorで前半2夜の3件ずつの定義と、後半の猶予倍率を調整できる。各列はLure／Provocation／Hijackを1件ずつ指定。元assetの猶予は変更されず、実行時だけ倍率を適用する。OFFなら既存Run Managerの作者比較リストを使う。下記の怪異比較ボタン／単独メニューを選ぶと自動的にOFFになるので、本編へ戻るときはONに戻す。本編／比較モードの変更はPlayを停止して行う。比較モード内の表現切替は引き続きPlay中も可能。

### 中断・再開／結果・自己ベスト（M4-01）

`Homecoming` の全4夜モードでPlayし、開始後にEsc→タイトルへ→確認。もう一度「続きから」で中断した夜のホールへ戻る（導入の途中だけは入口から）。完了した夜・累積時間・死亡数は維持する。Windows版は終了・再起動でも同じ。導入・ポーズ・ロードは計時せず、死亡と再挑戦は怪異3夜の時間に含む。全夜クリア後は今回の時間／死亡数と、別々に更新する最短時間／最少死亡数を表示し、次回の新規開始で導入省略を選べる。

- `テキスト編集 > 11_保存・続き・結果`：続き・確認・結果・エラー等の文章。タイトルの既存「はじめる」等は従来の01、設定項目名は04。数値や進行の判定は文章から独立している。
- 「新しく始める」は確認後に進行を置き換える。自己ベストと設定は残る。比較モード／PlayableDemoは保存対象外。
- 保存先はEditorが `artifacts/editor-saves/Homecoming`、Windows版が `%USERPROFILE%/AppData/LocalLow/DefaultCompany/GraduationProject/Homecoming`。相互に共有しない。
- 保存方式と故障時の扱いは [M4_SAVE_PLAN](M4_SAVE_PLAN.md)。読めないファイルは自動削除しない。異常終了では直前10秒程度の時間が未記録の場合がある。正常終了・ポーズでは直ちに保存する。

### 通常走行中の非常停止（M3-05）

`Homecoming` の全4夜モードで、通常帰宅を終えた翌夜から試せる。乗車して8階を押し、通常走行中に非常停止をクリックすると短く停止して自動再開する。約1/3で全6表現のどれかが追加され、その夜の通常3件も残る。追加は走行終了後に1件ずつ始まるため、当選しても停止した瞬間に出現するわけではない。怪異と怪異の間の通常走行でも同じ挙動。

停止中の連打や長押しは抽選を増やさない。導入夜・暴走を止める正解操作は抽選なし。`NormalJourney > Homecoming Campaign` のPlay中表示に「通常停止の抽選 / 待機中の追加」を表示する。抽選は外れることもあるので、1回だけで発生しなくても不具合ではない。比較モードでは追加抽選を行わない。

### 本編の誘引（Lure）演出

**柱／濡れの比較**: `Tools > Unity Agent > 怪異構成を開く` で `NormalJourney` のRun Managerへ移動する。「誘引：柱を試す」「誘引：濡れを試す」は遭遇リストの最初の誘引だけを差し替える。**Playを開始し、まだ怪異に到着する前に**切り替えると、停止時に元へ戻せる。停止中に切り替えた場合はシーンを保存する変更になる。既に出現中の怪異は切り替わらない。標準の順番では最初は柱、濡れは怪異夜2用。作者ローカルの比較リストは保存時の選択を保持し、全4夜の構成とは別に扱う。柱も濡れも開扉前に見た目を準備し、車内観察だけでは危険を開始しない。

濡れの見た目は `Assets/ApartmentVisuals/WetFloor.mat` の水たまりの広がり・濡れた床の明るさ・水面の滑らかさ・波紋の強さで調整。`Assets/Prefab/Anomalies/Lure_WetCorridor.prefab` は水面と廊下反射だけを持つ。通常床、Collider、移動速度には影響しない。`Assets/Data/lure_wet.asset` が猶予・正解・Prefabの定義。生成メニューを再実行して作者設定を作り直す必要はない。

廊下の配置や通常照明を変更したら、Homecomingを保存し、Edit Modeで `Tools > Unity Agent > 濡れた廊下の反射を更新`。128pxの室内Cubemapを再生成してPrefabへ接続する。ゲーム中の反射撮影や全体の描画設定変更は不要。減光後も反射画像は通常照明時のものなので、鏡像の正確な動的追従はしていない。生成前後で廊下／ホールの表示状態を復元し、作者のシーン値を保持する。

Hierarchy最上部の **演出調整_誘引の廊下** を選ぶ。Inspectorから照明の順序、照明間の遅れ、弱まる時間、残る明るさ、設備音量と立ち上がりを調整する。子の **奥の設備音_音源差替え可能** のAudio Sourceでクリップや位置を替えられる。ゲーム中のVolumeは親の「設備音量」で制御する。

初期値は奥→中央の2灯、遅れ0.35秒、減光0.65秒、元の6%まで、音量0.12・立ち上がり1.2秒。出入口に近い `HomeCorridor/InteriorVisuals/FixtureLight0` は帰還経路のため変化に含めない。誤降車で変化を開始し、ポーズで止まり、遭遇終了時に照明・発光面・音を戻す。車内で観察しているだけでは変化しない。

判定と時間制限は `BeatStateMachine`／既存BeatDefinition、見せ方は `LureCorridorPresentation` が担当する。表示速度を変えても猶予秒数は変わらない。既存デモとM2はこのcomponentを接続せず従来の色演出を維持。シーン再生成や演出追加メニューの再実行は通常の調整には不要。Play停止中に編集・保存する。

### 外から助けを求める声（M3-02）

`Tools > Unity Agent > 怪異構成を開く` の **挑発：外からの声を試す** で、最初のProvocationだけを差し替える。戻す場合は **挑発：設備放送を試す**。Play開始後、対象怪異に到着する前に切り替えると停止時に戻る。Edit Modeで変更・保存した場合は次の起動／ビルドにも反映する。

`Tools > Unity Agent > 声の怪異を編集` で専用Prefabを選ぶ。Voice Provocation Anomalyの日本語Inspectorに「最初の呼びかけ」「誤操作後の声」「音量」「最初の声までの秒数」「再生終了から次の声までの秒数」と2つの台本欄がある。**台本は制作メモ。文章の変更だけで既存WAVの発話は変わらない**。VOICEVOXや録音で作ったWAVをProjectへ入れ、クリップ欄へドラッグして差し替える。既存のゲーム内掲示文は変更していない。

Prefabを開いたHierarchyの `扉の外_音源とこもり` で位置、Audio Sourceの距離減衰、Audio Low Pass FilterのCutoff Frequencyを調整する。初期位置はドア外側、音量0.6、開始0.6秒、再生後の無音12秒、こもり1800Hz。走行中の通常設備音・階表示・モニターをこの演出から変更しない。

`Assets/Data/provocation_voice.asset` が正解・猶予の定義。無操作成功7秒、誤操作後の提示1.5秒＋猶予4秒の試作。長い音声へ差し替えたときは、開始遅延＋音声の長さ＋聞いた後の余白が無操作成功までに収まるよう調整する。仮音声クレジットと制作条件は `docs/licenses/OutsideVoice.txt`。HomecomingビルドにはREADMEとlicensesを自動同梱する。

### かご内の空間異常（M3-03）

確認シーンは `Assets/Scenes/Homecoming.unity`。Play前に `Tools > Unity Agent > 空間異常だけを試す（遭遇リスト変更）` を選ぶと、現在の遭遇リストを空間異常1件へ変更する。通常帰宅→翌夜、8階を選んで乗ると天井が遠ざかり階数が8を越える。非常停止で生還、最初の放置／閉で悪化し、その後の猶予内なら非常停止で救済できる。変更はUndo可能、Play中に選んだ場合は停止時に戻る。

複数の遭遇順を保つ比較は `怪異構成を開く > 乗っ取り：空間異常を試す`。このボタンは既にある最初のHijackだけを差し替えるため、声だけなどHijackがないリストでは単独確認メニューを使う。

`Tools > Unity Agent > 空間異常を編集` から専用Prefabを開く。Spatial Cabin Presentationの日本語Inspectorで高さ・伸びる秒数を編集する。判定・期限は `Assets/Data/hijack_space.asset`、共通怪異音量・階上昇間隔はシーンの `音・動作調整`。床と操作盤は動かさない。元天井のHierarchy名／配置／材質を変えた場合、Prefabの天井複製と参照パスも合わせる。生成メニューの再実行による作者設定の上書きは行わない。

## 編集の入口

1. `Assets/Scenes/PlayableDemo.unity` を開き、Playを停止する。
2. Hierarchy最上部の **★文章編集_ここから** を選ぶ。
3. Inspectorのカテゴリボタン、またはHierarchyの子オブジェクトを選び、文章欄を編集する。
4. **Ctrl+Sでシーンを保存**し、Playして表示を確認する。

見つからない場合は `Tools > Unity Agent > 文章編集を開く`。起動画面のCanvasはPlay時に生成されるが、その文章はこの常設オブジェクトに保存される。C#の編集は不要。

| カテゴリ | 編集する文章 |
|---|---|
| 01_起動画面 | タイトル、導入本文、操作説明、開始・設定・終了ボタン |
| 02_一時停止・確認 | ポーズ、やり直し・終了の確認、各ボタン |
| 03_翌夜・結果 | 翌夜、帰宅、読み込み、再プレイ・終了 |
| 04_設定画面 | 設定項目、オン／オフ、画面切替、説明・戻る |
| 05_案内・怪異・階数 | かご内の待機案内、停止を誘う文章と正体判明後の文章、帰宅表示、階数の書式、表札 |
| 06_操作盤・かご内表示 | ボタンの文字、定員、注意、案内見出しなど |
| 07_ロビー・廊下・その他表示 | 自宅・階の表示、旧試作用の非表示テキストなど |
| 08_掲示物の見出し・本文 | ロビーの3枚の掲示。本文・見出し・文字サイズ |

館内表示の項目には表示元のHierarchyパスを記載。「表示オブジェクトを確認」で場所を確認できる。非表示の旧試作用も残しているが、文章の変更だけではそれらが表示されるようにはならない。表札も使用している構成でのみ表示される。

## 保存・表示について

- Play中の変更は停止すると失われる。必ず停止中に執筆する。
- 文章は**シーンごと**に保存。デモはPlayableDemoを編集する。M1/M2は個別確認用で、自動同期しない。
- 空欄も保存される。空欄ボタンでもクリック機能は残るため、操作用ボタンには意味が分かる名前を付ける。
- ボタンの文章を変えても、行先階・操作・怪異の判定は変わらない。例えば「８」を「９」と書き換えても行先は８階。
- 動的な現在階は `{0}` を使用する（例：`{0} 階`）。不正な書式の場合は階数のみを表示する。
- メニューは改行・折り返しに対応し、長文はマウスホイールでスクロールできる。掲示・操作盤は固定の紙面／枠なので長すぎる文章は短縮や改行で調整する。
- 館内表示の紙面超過はInspectorに警告する。実際のGame Viewでも確認する。Rich Textタグ（`<size>`等）は表示装飾に使用できる。
- 文章編集欄が表示の正本。子のTMPや古いスクリプトの文字欄を直接編集すると、文章編集欄の内容が優先される。
- 3枚の掲示は `EntranceHall > 掲示物_文章編集はこちら` の各 `EditableNotice` からも編集可能。カテゴリ08は同じデータへの入口で、二重保存ではない。
- `Apply Apartment Visual Pass` は執筆済みの内容を保持する。シーン自体の作り直し・文章編集オブジェクトの削除は保持対象外。
- **既存のexeには反映されない**。保存後、`Tools > Unity Agent > Build Playable Demo Windows` で再ビルドする。配布にはexeと同じフォルダーのData等も必要。

## 現在のルールとの対応

執筆の内容はユーザーが決める。文章だけを調整する際の現行動作は以下。

- 普段の廊下を導入で覚え、８表示だけで降りない。違う廊下なら車内で「閉」。
- 放送・呼び声だけを根拠に非常停止しない。挑発では入力しない。
- ８を越える階数表示と、速く高くなる走行音の両方があれば非常停止。
- 誘引の救済は全身を車内へ戻して閉じ切る必要がある。外から閉めても正解にならない。

文章の分かりやすさ・初見理解・怖さは、実際のプレイ評価で確認する。

## 音と到着タイミングの調整

Hierarchyの **音・動作調整** を選ぶ。Inspectorの `Elevator Tuning` で、走行音・扉の動作音・到着チャイム・怪異の走行音・足音・換気音を0〜1で調整できる。個別AudioSourceのVolumeは演出用の増減で上書きされるため、この欄を使う。

同じ場所で **チャイムから開扉まで**（初期1.4秒）と **怪異の階数上昇間隔**（初期1秒）を編集できる。Play中に試す場合も、保存したい値はPlay停止後に設定し直してCtrl+S。設定画面の「音量」は全体音量で、このInspectorの各音の比率と併用する。

文章編集rootを「テキスト編集」などに改名しても編集入口は識別される。上の `★文章編集_ここから` は初期名称であり、ユーザーが付けた名前が優先される。

## かご内モニターのスライドショー

Hierarchyの `テキスト編集`（初期名は `★文章編集_ここから`）→ `05_案内・怪異・階数` を選ぶ。「かご内モニター：最初の文言」が従来の文章で、その直下に追加文・時間設定がある。「モニター本体を選択」からも同じ設定を編集できる。

- **追加の文言（＋で追加）**: リストを開き＋で追加、−で削除、ドラッグで順番を変更する。改行も保存される。最初の文言→追加リストの上から順→最初の文言へ循環。
- **1枚の表示時間（秒）**: 完全に表示してからフェードアウト開始まで。初期値5秒。
- **片道のフェード時間（秒）**: 消える時間と現れる時間それぞれ。初期値0.5秒なら切替全体は約1秒。大きいほどゆっくり、0なら瞬時切替。

文章は作者が執筆する方針なので追加リストは初期0件。既存文は保持され、＋で文章を増やすとスライドショーになる。1枚だけなら点滅させず常時表示。空欄を追加すると空白の1枚として扱う。文字枠は従来のままなので短い文と改行で調整する。

怪異の案内表示が通常スライドより優先し、終了後に最初の文言から再開する。怪異の提示時間・対応ルールはこの設定では変わらない。一時停止中は進行しない。Play停止中に編集してCtrl+Sでシーンを保存する。M1/M2/PlayableDemoの設定は各シーン独立、既存exeへ反映するには再buildが必要。
